export default function OrganizationActivityPage() {
  return (
    <p className="home__empty-state">
      The unified activity timeline (calls, emails, notes, system events) lands alongside
      email sync and call notes in Stage 2+.
    </p>
  );
}
